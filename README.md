# 🤖 JivoChat-Telegram Bridge

Автоматическая пересылка заявок из JivoChat в группу Telegram.

## 📋 Описание

Этот сервис принимает webhooks от JivoChat и автоматически отправляет красиво отформатированные уведомления в вашу Telegram группу.

**Поддерживаемые события:**
- ✅ Завершенные чаты (`chat_finished`)
- 📬 Оффлайн сообщения (`offline_message`)
- 👋 Принятые чаты (`chat_accepted`)
- 🔄 Обновления информации (`chat_updated`)

---

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.9 или выше
- pip (менеджер пакетов Python)

### 1️⃣ Установка зависимостей

```bash
cd jivochat-telegram-bridge
pip install -r requirements.txt
```

### 2️⃣ Настройка конфигурации

Ваши данные уже настроены в файле `.env`:

```env
TELEGRAM_BOT_TOKEN=8560374126:AAGyGmyhK1NgdHfIhxW5jRxzaT6NNTTH_xk
TELEGRAM_CHAT_ID=-5069187781
```

> ⚠️ **Важно:** Не публикуйте файл `.env` в публичных репозиториях!

### 3️⃣ Запуск сервера

```bash
python app.py
```

Сервер запустится на `http://localhost:5000`

---

## 🧪 Тестирование

### Проверка работоспособности

Откройте в браузере:
```
http://localhost:5000/health
```

Должен вернуться JSON с статусом `ok`.

### Отправка тестового сообщения в Telegram

Откройте в браузере:
```
http://localhost:5000/test
```

Если всё настроено правильно, в вашей Telegram группе появится тестовое сообщение! ✅

### Тестирование webhook от JivoChat

Отправьте тестовый POST-запрос:

```bash
curl -X POST "http://localhost:5000/webhook/jivochat" \
  -H "Content-Type: application/json" \
  -d '{
    "event_name": "chat_finished",
    "visitor": {
      "name": "Иван Иванов",
      "email": "ivan@example.com",
      "phone": "+79001234567"
    },
    "agent": {
      "name": "Менеджер Петр"
    },
    "chat_log": [
      {
        "timestamp": 1638360000,
        "sender": {"name": "Иван Иванов"},
        "text": "Здравствуйте, интересует ваш продукт"
      },
      {
        "timestamp": 1638360060,
        "sender": {"name": "Менеджер Петр"},
        "text": "Здравствуйте! С удовольствием расскажу подробнее."
      }
    ]
  }'
```

---

## 🌐 Развертывание в интернете

Для того чтобы JivoChat мог отправлять webhooks на ваш сервер, он должен быть доступен из интернета по HTTPS.

### Вариант 1: REG.RU VPS (Для российских пользователей) ⭐

**Рекомендуется, если:**
- Вы находитесь в России
- Нужен российский хостинг
- Требуется полный контроль над сервером

📖 **[Подробная инструкция по развертыванию на REG.RU →](DEPLOY_REGRU.md)**

📋 **[Краткая инструкция для быстрого старта →](QUICK_DEPLOY.md)**

**Быстрый старт:**
1. Закажите VPS на [reg.ru/vps](https://www.reg.ru/vps/)
2. Настройте домен (A-запись на IP VPS)
3. Подключитесь по SSH и выполните:
   ```bash
   sudo bash setup_server.sh    # Базовая настройка
   sudo bash setup_app.sh       # Настройка приложения
   sudo bash setup_nginx.sh ваш-домен.ru  # Nginx
   sudo certbot --nginx -d ваш-домен.ru   # SSL
   ```

**Стоимость:** ~300-500₽/мес

---

### Вариант 2: Heroku (Международный, простой)

1. Зарегистрируйтесь на [Heroku](https://www.heroku.com/)
2. Установите [Heroku CLI](https://devcenter.heroku.com/articles/heroku-cli)
3. Выполните:

```bash
# Инициализация git репозитория (если еще не сделано)
git init
git add .
git commit -m "Initial commit"

# Вход в Heroku
heroku login

# Создание приложения
heroku create your-app-name

# Установка переменных окружения
heroku config:set TELEGRAM_BOT_TOKEN=8560374126:AAGyGmyhK1NgdHfIhxW5jRxzaT6NNTTH_xk
heroku config:set TELEGRAM_CHAT_ID=-5069187781

# Деплой
git push heroku main
```

Ваш webhook URL: `https://your-app-name.herokuapp.com/webhook/jivochat`

### Вариант 2: Ngrok (Для тестирования)

Если хотите быстро протестировать без развертывания:

1. Установите [ngrok](https://ngrok.com/)
2. Запустите сервер локально: `python app.py`
3. В другом терминале: `ngrok http 5000`
4. Используйте HTTPS URL от ngrok в настройках JivoChat

> ⚠️ Ngrok подходит только для тестирования! Для продакшена используйте Heroku или VPS.

### Вариант 3: VPS сервер

Подробная инструкция по развертыванию на VPS есть в основном руководстве.

---

## ⚙️ Настройка JivoChat

1. Войдите в ваш аккаунт JivoChat
2. Перейдите: **Управление** → **Каналы** → **Настройки** → **Настройки интеграции для разработчиков**
3. Включите webhook и введите URL:
   ```
   https://your-domain.com/webhook/jivochat
   ```
4. Сохраните настройки

---

## 📁 Структура проекта

```
jivochat-telegram-bridge/
├── app.py                 # Основное приложение Flask
├── requirements.txt       # Python зависимости
├── .env                   # Конфигурация (не коммитить!)
├── .env.example          # Пример конфигурации
├── .gitignore            # Игнорируемые файлы
├── Procfile              # Для Heroku
├── runtime.txt           # Версия Python для Heroku
└── README.md             # Этот файл
```

---

## 🔧 API Endpoints

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/` | GET | Главная страница с информацией |
| `/webhook/jivochat` | POST | Webhook для JivoChat |
| `/health` | GET | Проверка работоспособности |
| `/test` | GET | Отправка тестового сообщения |

---

## 📝 Примеры сообщений

### Завершенный чат
```
🆕 Новая заявка из JivoChat

👤 Клиент: Иван Иванов
📧 Email: ivan@example.com
📱 Телефон: +79001234567

👨‍💼 Оператор: Менеджер Петр

💬 Сообщения (2):
[14:30] Иван Иванов: Здравствуйте...
[14:31] Менеджер Петр: Здравствуйте!...
```

### Оффлайн сообщение
```
📬 Оффлайн сообщение из JivoChat

👤 От: Мария Петрова
📧 Email: maria@example.com
📱 Телефон: +79009876543

💬 Сообщение:
Добрый день, хочу узнать о ваших услугах...
```

---

## 🛠️ Настройка

### Изменение списка событий

В файле `app.py` найдите:

```python
EVENTS_TO_NOTIFY = ['chat_finished', 'offline_message', 'chat_accepted', 'chat_updated']
```

Уберите события, о которых не хотите получать уведомления.

### Изменение формата сообщений

Отредактируйте функции `format_chat_finished()`, `format_offline_message()` и др. в файле `app.py`.

---

## 🐛 Решение проблем

### Сообщения не приходят в Telegram

1. Проверьте, что бот добавлен в группу
2. Убедитесь, что `chat_id` правильный (должен начинаться с `-`)
3. Проверьте токен бота

### JivoChat не отправляет webhooks

1. Убедитесь, что URL доступен по HTTPS
2. Проверьте логи сервера: `tail -f jivochat_bridge.log`
3. Попробуйте отправить тестовый запрос через curl

### Ошибка 401 от Telegram API

Неправильный токен бота. Проверьте `.env` файл.

---

## 📊 Логи

Все события записываются в файл `jivochat_bridge.log`:

```bash
tail -f jivochat_bridge.log
```

---

## 🔐 Безопасность

- ✅ Файл `.env` добавлен в `.gitignore`
- ✅ Токены хранятся в переменных окружения
- ✅ HTTPS обязателен для продакшена
- ⚠️ Не публикуйте токены в открытом доступе!

---

## 📚 Дополнительные ресурсы

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [JivoChat API Documentation](https://www.jivochat.com/api/)
- [Flask Documentation](https://flask.palletsprojects.com/)

---

## 💡 Поддержка

Если возникли вопросы или проблемы:
1. Проверьте логи в `jivochat_bridge.log`
2. Попробуйте тестовый endpoint `/test`
3. Убедитесь, что все зависимости установлены

---

## ✨ Что дальше?

После успешного запуска можете добавить:
- 💾 Сохранение заявок в базу данных
- 📈 Статистику и аналитику
- 🔔 Интеграцию с другими сервисами
- 🎨 Кастомизацию внешнего вида сообщений

---

**Готово! Ваш бридж между JivoChat и Telegram настроен и готов к работе! 🎉**
