# 🚀 Развертывание на Fly.io - Пошаговая инструкция

Детальное руководство по развертыванию JivoChat-Telegram Bridge на Fly.io.

---

## ✅ Преимущества Fly.io

- 🆓 **Бесплатный план** (достаточно для этого проекта)
- ⚡ **Не засыпает** (в отличие от Render)
- 🔒 **Автоматический SSL**
- 🌍 **Глобальная сеть** дата-центров
- 🔄 **Автоматические обновления** при git push

---

## 📋 Что вам понадобится

- Аккаунт на Fly.io (создадим)
- Git установлен на компьютере
- Терминал (macOS Terminal)
- 5-10 минут времени

---

## Шаг 1: Установка Fly CLI

Откройте терминал и выполните:

```bash
# Установка через Homebrew (для Mac)
brew install flyctl
```

**Проверка установки:**
```bash
flyctl version
```

Должна отобразиться версия, например: `flyctl v0.1.xxx`

---

## Шаг 2: Регистрация на Fly.io

```bash
# Регистрация нового аккаунта
flyctl auth signup
```

Это откроет браузер для регистрации:
1. Заполните форму регистрации
2. Подтвердите email
3. Добавьте банковскую карту (для верификации, **не будет списаний на free плане**)

**Если уже есть аккаунт:**
```bash
flyctl auth login
```

---

## Шаг 3: Подготовка проекта

### 3.1 Переход в директорию проекта

```bash
cd /Users/asanhajretdinov/.gemini/antigravity/scratch/jivochat-telegram-bridge
```

### 3.2 Проверка файлов

Убедитесь, что есть все необходимые файлы:

```bash
ls -la
```

Должны быть:
- ✅ `app.py`
- ✅ `requirements.txt`
- ✅ `Dockerfile` (уже создан)
- ✅ `.env` (но не будем его коммитить!)

### 3.3 Инициализация Git (если еще не сделано)

```bash
# Проверка, инициализирован ли Git
git status
```

Если ошибка "not a git repository", выполните:

```bash
git init
git add .
git commit -m "Initial commit for Fly.io deployment"
```

---

## Шаг 4: Создание приложения на Fly.io

```bash
flyctl launch
```

Fly CLI задаст несколько вопросов. Отвечайте так:

### Вопросы и ответы:

**1. Choose an app name:**
```
jivochat-telegram-bridge
```
(или придумайте свое уникальное имя)

**2. Choose a region for deployment:**
```
ams (Amsterdam, Netherlands)
```
(ближайший к России регион с хорошей скоростью)

Другие опции:
- `fra` - Frankfurt, Germany
- `waw` - Warsaw, Poland

**3. Would you like to set up a PostgreSQL database?**
```
No
```

**4. Would you like to set up an Upstash Redis database?**
```
No
```

**5. Would you like to deploy now?**
```
No
```
(сначала настроим переменные окружения)

---

## Шаг 5: Настройка переменных окружения

Fly.io создал файл `fly.toml`. Теперь добавим секреты:

```bash
# Добавление токена Telegram бота
flyctl secrets set TELEGRAM_BOT_TOKEN=8560374126:AAGyGmyhK1NgdHfIhxW5jRxzaT6NNTTH_xk

# Добавление ID группы
flyctl secrets set TELEGRAM_CHAT_ID=-5069187781
```

После добавления каждого секрета вы увидите подтверждение.

**Проверка секретов:**
```bash
flyctl secrets list
```

---

## Шаг 6: Проверка конфигурации

Откройте файл `fly.toml` для проверки:

```bash
nano fly.toml
```

Содержимое должно выглядеть примерно так:

```toml
app = "jivochat-telegram-bridge"
primary_region = "ams"

[build]

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 256
```

> [!IMPORTANT]
> Убедитесь, что `internal_port = 8080` (как в Dockerfile)

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

---

## Шаг 7: Развертывание! 🚀

Теперь запускаем деплой:

```bash
flyctl deploy
```

Процесс займет 2-5 минут. Вы увидите:
1. ✅ Building image
2. ✅ Pushing image to registry
3. ✅ Deploying
4. ✅ Monitoring deployment

При успешном завершении увидите:
```
✓ visit your newly deployed app at https://jivochat-telegram-bridge.fly.dev/
```

---

## Шаг 8: Проверка работы

### 8.1 Проверка через браузер

Откройте:
```
https://jivochat-telegram-bridge.fly.dev/
```

Должна открыться главная страница приложения!

### 8.2 Проверка health endpoint

```bash
curl https://jivochat-telegram-bridge.fly.dev/health
```

Должен вернуться JSON:
```json
{
  "status": "ok",
  "service": "JivoChat-Telegram Bridge",
  "telegram_bot_configured": true,
  "telegram_chat_configured": true
}
```

### 8.3 Тест отправки в Telegram

Откройте в браузере:
```
https://jivochat-telegram-bridge.fly.dev/test
```

Проверьте вашу Telegram группу - должно прийти тестовое сообщение! ✅

---

## Шаг 9: Настройка JivoChat

Теперь настройте webhook в JivoChat:

1. Войдите в аккаунт JivoChat
2. **Управление** → **Каналы** → **Настройки** → **Настройки интеграции для разработчиков**
3. Включите webhook
4. Введите URL:
   ```
   https://jivochat-telegram-bridge.fly.dev/webhook/jivochat
   ```
5. Сохраните настройки

---

## Шаг 10: Тестирование webhook

Отправьте тестовый запрос:

```bash
curl -X POST "https://jivochat-telegram-bridge.fly.dev/webhook/jivochat" \
  -H "Content-Type: application/json" \
  -d '{
    "event_name": "chat_finished",
    "visitor": {
      "name": "Тестовый Клиент",
      "email": "test@example.com",
      "phone": "+79001234567"
    },
    "agent": {
      "name": "Менеджер Иван"
    },
    "chat_log": [
      {
        "timestamp": 1638360000,
        "sender": {"name": "Клиент"},
        "text": "Здравствуйте, интересует ваш продукт"
      }
    ]
  }'
```

Проверьте Telegram группу - должно прийти уведомление! 🎉

---

## 🔧 Полезные команды Fly.io

### Просмотр логов

```bash
# Логи в реальном времени
flyctl logs

# Последние 100 строк
flyctl logs -n 100
```

### Статус приложения

```bash
flyctl status
```

### Информация о приложении

```bash
flyctl info
```

### Открыть приложение в браузере

```bash
flyctl open
```

### Открыть dashboard

```bash
flyctl dashboard
```

### Масштабирование

```bash
# Увеличить память (если нужно)
flyctl scale memory 512

# Количество инстансов
flyctl scale count 1
```

---

## 🔄 Обновление приложения

Когда нужно обновить код:

```bash
# 1. Внесите изменения в код
# 2. Закоммитьте изменения
git add .
git commit -m "Update application"

# 3. Задеплойте
flyctl deploy
```

Fly.io автоматически соберет новый образ и обновит приложение!

---

## 📊 Мониторинг

### Просмотр метрик

```bash
flyctl dashboard
```

В веб-интерфейсе вы увидите:
- CPU usage
- Memory usage
- Network traffic
- Request count

### Настройка алертов

В dashboard Fly.io можете настроить уведомления на email при проблемах.

---

## 💰 Бесплатный план - лимиты

Fly.io дает бесплатно:
- ✅ 3 приложения
- ✅ 3GB persistent storage
- ✅ 160GB исходящего трафика/мес
- ✅ 2,340 машино-часов/мес (достаточно для 1 приложения 24/7)

**Для нашего приложения хватит с запасом!** 🎉

---

## 🛠️ Решение проблем

### Приложение не запускается

```bash
# Проверить логи
flyctl logs

# Проверить статус
flyctl status

# Перезапустить
flyctl apps restart jivochat-telegram-bridge
```

### Изменить переменные окружения

```bash
# Обновить секрет
flyctl secrets set TELEGRAM_BOT_TOKEN=новый_токен

# Удалить секрет
flyctl secrets unset TELEGRAM_BOT_TOKEN
```

### Проблемы с Dockerfile

Убедитесь, что `internal_port` в `fly.toml` совпадает с `EXPOSE` в `Dockerfile`:

**Dockerfile:**
```dockerfile
EXPOSE 8080
```

**fly.toml:**
```toml
[http_service]
  internal_port = 8080
```

### Webhook не работает

1. Проверьте, что приложение запущено: `flyctl status`
2. Проверьте логи: `flyctl logs`
3. Попробуйте тестовый endpoint: `curl https://ваше-приложение.fly.dev/health`

---

## 🔐 Безопасность

### Не коммитить секреты!

Убедитесь, что `.gitignore` содержит:
```
.env
venv/
__pycache__/
*.pyc
```

### Проверка что .env не в Git

```bash
git status
```

Если `.env` в списке, удалите его:
```bash
git rm --cached .env
git commit -m "Remove .env from git"
```

---

## 📱 Управление через веб-интерфейс

Можно управлять приложением через [fly.io/dashboard](https://fly.io/dashboard):
- Просмотр метрик
- Управление переменными
- Просмотр логов
- Масштабирование

---

## ✅ Чек-лист развертывания

- [ ] Установлен flyctl
- [ ] Создан аккаунт на Fly.io
- [ ] Выполнен `flyctl launch`
- [ ] Добавлены секреты (TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID)
- [ ] Выполнен `flyctl deploy`
- [ ] Проверена работа через браузер
- [ ] Протестирован /health endpoint
- [ ] Протестирован /test endpoint (сообщение пришло в Telegram)
- [ ] Настроен webhook в JivoChat
- [ ] Протестирован webhook

---

## 🎉 Готово!

Ваше приложение развернуто на Fly.io и работает!

**Ваш webhook URL для JivoChat:**
```
https://jivochat-telegram-bridge.fly.dev/webhook/jivochat
```

**Полезные ссылки:**
- 🌐 Приложение: https://jivochat-telegram-bridge.fly.dev/
- 📊 Dashboard: https://fly.io/dashboard
- 📖 Документация: https://fly.io/docs/

---

## 🆘 Нужна помощь?

**Команда для получения помощи:**
```bash
flyctl help
```

**Документация Fly.io:**
https://fly.io/docs/

**Сообщество:**
https://community.fly.io/

---

Если возникнут вопросы - пишите! 💪
